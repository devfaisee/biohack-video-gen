[providers]
node = true

[phases.setup]
aptPkgs = ["ffmpeg", "fontconfig", "fonts-liberation", "fonts-dejavu", "fonts-noto"]

[phases.install]
cmds = ["cd server && npm install"]

[start]
cmd = "cd server && node server.js"
